@model CyberWatchAnalytics.ViewModels.VulnerabilidadCreateViewModel

@{
    ViewData["Title"] = "Nueva Vulnerabilidad";
}

<div class="container-fluid">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2>Nueva Vulnerabilidad</h2>
            <p class="text-muted mb-0">
                Complete la información de la vulnerabilidad detectada.
            </p>
        </div>

        <a asp-action="Index" class="btn btn-outline-secondary">
            Volver
        </a>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body">

            <form asp-action="Create" method="post">

                <div asp-validation-summary="ModelOnly"
                     class="text-danger mb-3"></div>

                <div class="row">

                    <div class="col-md-6 mb-3">
                        <label asp-for="IdActivo" class="form-label"></label>

                        <select asp-for="IdActivo"
                                asp-items="Model.Activos"
                                class="form-select">
                            <option value="">Seleccione...</option>
                        </select>

                        <span asp-validation-for="IdActivo"
                              class="text-danger"></span>
                    </div>

                    <div class="col-md-3 mb-3">
                        <label asp-for="Severidad" class="form-label"></label>

                        <select asp-for="Severidad" class="form-select">
                            <option value="Baja">Baja</option>
                            <option value="Media">Media</option>
                            <option value="Alta">Alta</option>
                            <option value="Crítica">Crítica</option>
                        </select>

                        <span asp-validation-for="Severidad"
                              class="text-danger"></span>
                    </div>

                    <div class="col-md-3 mb-3">
                        <label asp-for="EstadoMitigacion" class="form-label"></label>

                        <select asp-for="EstadoMitigacion" class="form-select">
                            <option value="Pendiente">Pendiente</option>
                            <option value="En análisis">En análisis</option>
                            <option value="En mitigación">En mitigación</option>
                            <option value="Mitigada">Mitigada</option>
                        </select>

                        <span asp-validation-for="EstadoMitigacion"
                              class="text-danger"></span>
                    </div>

                </div>

                <div class="mb-3">
                    <label asp-for="Descripcion" class="form-label"></label>

                    <textarea asp-for="Descripcion"
                              class="form-control"
                              rows="5"
                              placeholder="Describa la vulnerabilidad detectada"></textarea>

                    <span asp-validation-for="Descripcion"
                          class="text-danger"></span>
                </div>

                <hr />

                <button type="submit" class="btn btn-primary">
                    Guardar Vulnerabilidad
                </button>

                <a asp-action="Index" class="btn btn-secondary">
                    Cancelar
                </a>

            </form>

        </div>
    </div>

</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}