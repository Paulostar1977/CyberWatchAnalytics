@model IEnumerable<CyberWatchAnalytics.Models.Vulnerabilidad>

@{
    ViewData["Title"] = "Vulnerabilidades";
}

<div class="container-fluid">

    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2>Vulnerabilidades</h2>
            <p class="text-muted mb-0">
                Administración de vulnerabilidades detectadas en los activos tecnológicos.
            </p>
        </div>

        <a asp-action="Create" class="btn btn-primary">
            Nueva Vulnerabilidad
        </a>
    </div>

    @if (TempData["MensajeExito"] != null)
    {
        <div class="alert alert-success">
            @TempData["MensajeExito"]
        </div>
    }

    @if (TempData["MensajeError"] != null)
    {
        <div class="alert alert-danger">
            @TempData["MensajeError"]
        </div>
    }

    <div class="card shadow-sm border-0">
        <div class="card-body">
            <div class="table-responsive">

                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>Activo</th>
                            <th>Descripción</th>
                            <th>Severidad</th>
                            <th>Estado de mitigación</th>
                            <th>Fecha de detección</th>
                            <th class="text-end">Acciones</th>
                        </tr>
                    </thead>

                    <tbody>
                        @foreach (var vulnerabilidad in Model)
                        {
                            <tr>
                                <td>
                                    @vulnerabilidad.IdActivoNavigation.NombreActivo
                                </td>

                                <td>
                                    @vulnerabilidad.Descripcion
                                </td>

                                <td>
                                    @switch (vulnerabilidad.Severidad)
                                    {
                                        case "Crítica":
                                            <span class="badge bg-danger">Crítica</span>
                                            break;

                                        case "Alta":
                                            <span class="badge bg-warning text-dark">Alta</span>
                                            break;

                                        case "Media":
                                            <span class="badge bg-primary">Media</span>
                                            break;

                                        default:
                                            <span class="badge bg-secondary">Baja</span>
                                            break;
                                    }
                                </td>

                                <td>
                                    @vulnerabilidad.EstadoMitigacion
                                </td>

                                <td>
                                    @vulnerabilidad.FechaDeteccion.ToString("dd-MM-yyyy")
                                </td>

                                <td class="text-end">
                                    <a asp-action="Edit"
                                       asp-route-id="@vulnerabilidad.IdVulnerabilidad"
                                       class="btn btn-outline-primary btn-sm">
                                        Editar
                                    </a>

                                    <a asp-action="Delete"
                                       asp-route-id="@vulnerabilidad.IdVulnerabilidad"
                                       class="btn btn-outline-danger btn-sm">
                                        Eliminar
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>

            </div>
        </div>
    </div>

</div>